<?xml version="1.0" encoding="UTF-8" ?>
 <!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.dao.MemberDao">

	<!-- 회원가입 -->
	<insert id="logininsert" parameterType="member">
		insert into member values(#{memail},#{mpw},#{mname},#{mphone},#{mage},#{mgender},#{maddress},#{mcareer},
		#{meducation},#{mmajor},#{mcertification},#{mlang},#{mdb},sysdate(),default)
	</insert>
	
	<!-- 로그인 -->
	<select id="dbselectone" resultType="member" >
		select * from member where memail = #{memail} and mdrop = 'N'
	</select>
</mapper>
